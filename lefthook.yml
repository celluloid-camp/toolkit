pre-commit:
  jobs:
    - name: python-format
      glob: "*.py"
      run: uv run --extra dev black {staged_files}
    - name: python-lint
      glob: "*.py"
      run: uv run --extra dev flake8 {staged_files}
